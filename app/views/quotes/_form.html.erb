<%= form_with(model: quote, local: true) do |form| %>
  <% if quote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(quote.errors.count, "error") %> prohibited this quote from being saved:</h2>

      <ul>
      <% quote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :character %>
    <%= form.text_field :character, id: :quote_character %>
  </div>

  <div class="field">
    <%= form.label :quote %>
    <%= form.text_area :quote, id: :quote_quote %>
  </div>

  <div class="field">
    <%= form.label :location %>
    <%= form.text_field :location, id: :quote_location %>
  </div>

  <div class="field">
    <%= form.label :witness %>
    <%= form.text_field :witness, id: :quote_witness %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
